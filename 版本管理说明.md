# 本地 Git 版本管理

本目录是独立 Git 仓库。运行代码、配置、公共 assets、纸样模板和常规使用文档纳入版本管理；outputs、测试脚本、验收报告、Python 缓存、虚拟环境和本地 SMPL 模型不纳入。忽略规则不会删除本地文件；仿真产物和本地测试资料如需保留，请另行备份。

已进入历史提交的测试与报告不会因忽略规则而从历史消失；当前版本通过普通提交移除跟踪，不强制推送或重写历史。运行时输入校验、仿真状态判定和必要的错误报告仍属于运行功能，继续保留。

## 日常使用

在本目录执行：

```bash
git status
git diff
git add <需要保存的文件>
git diff --cached
git commit -m "描述本次修改"
git log --oneline -10
```

建议在新分支开发：`git switch -c fix/simulation-placement`。

## 远程仓库（可选）

本地初始化和提交不需要远程仓库。需要远程备份/协作时，在 GitHub、GitLab 或 Gitee 创建空仓库；不要自动生成 README、.gitignore 或其他初始提交，以免与本地历史冲突。确认仓库可见性及共享文件范围后，在本目录执行：

```bash
git remote add origin <你的远程仓库地址>
git push -u origin HEAD
```

远程地址目前未配置，不会自动上传任何文件。后续在已建立上游关系的分支执行 `git push` 即可。
