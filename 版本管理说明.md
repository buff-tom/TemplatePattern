# 本地 Git 版本管理

本目录是独立 Git 仓库。代码、配置、公共 assets、纸样模板和文档纳入版本管理；outputs、Python 缓存、虚拟环境和本地 SMPL 模型不纳入。忽略规则不会删除本地文件；仿真产物如需保留，请另行备份。

首次提交保存的是当前开发基线，不代表完整仿真已通过质量验收。已知限制见 `修复与验证_2026-09-11.md`。

## 日常使用

在本目录执行：

```bash
git status
git diff
git add <需要保存的文件>
git diff --cached
git commit -m "描述本次修改"
git log --oneline -10
```

建议在新分支开发：`git switch -c fix/simulation-placement`。

## 远程仓库（可选）

本地初始化和提交不需要远程仓库。需要远程备份/协作时，在 GitHub、GitLab 或 Gitee 创建空仓库；不要自动生成 README、.gitignore 或其他初始提交，以免与本地历史冲突。确认仓库可见性及共享文件范围后，在本目录执行：

```bash
git remote add origin <你的远程仓库地址>
git push -u origin HEAD
```

远程地址目前未配置，不会自动上传任何文件。后续在已建立上游关系的分支执行 `git push` 即可。
