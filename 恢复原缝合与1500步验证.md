# 恢复原缝合、动态摆放与 1500 步验收

本轮只修改 final；v1/v2 仅只读对照，SMPL 模型仅只读加载。未覆盖历史 outputs，未重写 Git 历史，修改仍在工作区，尚未提交。

## 已修改与恢复

- 选模：重新使用标准尺码及 150/160cm、高壮体型虚拟锚点。优先选择六项容量均能覆盖输入的模板；不能覆盖时用原木桶回退思想，最大化最弱约束的容量差，再用归一化加权距离破同分。报告保留选中锚点、实际模板尺码及回退原因。不因“没有满足的尺码”停止 Stage 1。
- 相比 v1，明确区别是：v1 在全局范围内使用普通最近锚点；当前按本次要求在范围内也优先满足全部上限，而不是让较小的平均最近尺码压过最大需求。
- 缝合：删除新增缝线重采样模块，恢复原 reference 的全部 stitches、边索引、方向、顶点和边界；撤回我对 PieceSplitter/StitchBuilder 的方向、腋下连接及领片拆分等修改。Stage 2 不再调用新缝合重建路径。
- 材料：不改变拉伸、弯曲、阻尼、密度、摩擦、厚度等材料物理值；撤回关闭 attachment constraint 的改动。texture DPI 保持 150，保留渲染依赖兼容修复。
- 摆放：躯干相对参考人体胸部中心、肩宽和身高移动；领片相对颈部移动；袖片/袖口沿目标肩到腕轴的位置和角度变化；前后 Z 根据身体表面变化，保持原参考间隙。不修改任何片的局部顶点或 stitches。输入参考人体时为恒等变换。
- 补充安装包包含 `assets/placement_reference.json`，避免安装后缺少摆放校准文件。

## 重要边界

本次按“恢复原缝合”的要求恢复的是原 reference 仿真纸样。**Stage 2 目前保留 reference 的二维几何，并未把 Stage 1 的任意新轮廓重新映射到 reference 顶点上。** Stage 1 的选模、人体目标正常变化，Stage 2 人体和摆放动态变化；不能把这描述成 Stage 1/Stage 2 二维轮廓完全一致。要实现后者，需要另行建立保留现有边索引与缝合关系的几何映射，不能再次用重采样修改缝合图。

“任意尺寸都有模板回退/Stage 1 输出”不等于“任意人体参数均能精确拟合 SMPL 或仿真均能收敛”。缺少模型、非有限/负数尺寸及引擎故障仍需明确报错，不能伪造合格产物。

## 验证结果

- 8 项协议、原缝合精确一致性、参考人体恒等摆放、体型变化不改缝合、输入回退等测试通过。
- 选模测试覆盖 1500～2000mm 每 1mm、4 种腰围，共 2004 组；每组均返回已有模板。
- 新生成 32 组长短袖 Stage 1 和转换任务包，报告：`outputs/validation/restored_1500_inputs/height_validation_report.json`。
- 主机 RTX 5090 上串行运行长短袖，均实际执行 1500 步并完成 OBJ、正反面渲染及 ZIP 归档。

| 款式 | 非静止顶点 | 穿体计数 | 自交计数 | 质量验收 |
| --- | --- | --- | --- | --- |
| 短袖 | 7448 / 9135 | 671 | 1060 | failed |
| 长袖 | 9716 / 12522 | 379 | 672 | failed |

这表示运行链路已完成，但完整成衣质量仍未通过。没有更改质量阈值，也没有把 failed 改为 completed。

## 产物与复跑命令

本轮产物根目录：`outputs/validation/restored_gpu_1500/`。

- `gpu_validation_report.json`：两款汇总。
- `short.log`、`long.log`：运行日志。
- `short/`、`long/`：分别包含 `stage2_manifest.json`、`body/`、`input/`、`simulation/`、`stage2_results.zip`。
- `simulation/simulation_report.json`：最终统计；`simulation/sim_props.yaml`：实际使用的 1500 步配置。
- `simulation/boxmesh.obj`、`sim.obj`、`render_front.png`、`render_back.png` 均非空，可用于人工检查，但不是合格成衣证明。

在 SewingPattern 目录运行，设置 SMPL_MODEL_DIR 为男体模型目录，并使用新输出目录：

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final.tests.validate_gpu \
  --height-report-dir TemplatePattern_final/outputs/validation/restored_1500_inputs \
  --output-dir TemplatePattern_final/outputs/validation/restored_gpu_1500_rerun \
  --smpl-model-dir "$SMPL_MODEL_DIR" \
  --max-sim-steps 1500
```

常规 Stage 2 同样添加 `--max-sim-steps 1500 --archive`；本次没有改动全局默认步数。此报告取代先前文档中关于新缝合算法仍在使用的描述。
