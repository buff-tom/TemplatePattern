# TemplatePattern_final 两阶段运行与 Docker 部署

当前同时支持：

- 本机 `uv` 虚拟环境：推荐开发和调试；
- 基于 CUDA 的 Docker 镜像：推荐迁移和固定运行环境。

`uv` 负责 Python 解释器、虚拟环境和 Python 包；CUDA Toolkit、NVIDIA 驱动、编译器及系统图形库仍由操作系统或 Docker 基础镜像提供。

## 一、uv 本地环境

```bash
cd /home/user/buff-tomma/SewingPattern
curl -LsSf https://astral.sh/uv/install.sh | sh
uv python install 3.12.3
uv venv --python 3.12.3 .venv
uv pip install --python .venv/bin/python \
  --index https://download.pytorch.org/whl/cpu torch==2.8.0
uv pip install --python .venv/bin/python -r TemplatePattern_final/requirements-runtime.txt
uv pip install --python .venv/bin/python -e TemplatePattern_final
```

Stage 2 默认使用 MHR v1.0.1 LOD1 TorchScript 模型：

```bash
wget -O /tmp/mhr-assets.zip \
  https://github.com/facebookresearch/MHR/releases/download/v1.0.1/assets.zip
mkdir -p TemplatePattern_final/models/mhr
unzip -j /tmp/mhr-assets.zip assets/mhr_model.pt \
  -d TemplatePattern_final/models/mhr
```

并准备 GarmentCode、NvidiaWarp-GarmentCode：

```bash
export TEMPLATEPATTERN_GARMENTCODE_ROOT=/path/to/GarmentCode
export TEMPLATEPATTERN_WARP_ROOT=/path/to/NvidiaWarp-GarmentCode
export TEMPLATEPATTERN_MHR_MODEL_DIR=/path/to/mhr-model-directory
.venv/bin/python /path/to/NvidiaWarp-GarmentCode/build_lib.py --cuda_path=/usr/local/cuda
```

MHR 目录必须包含 `mhr_model.pt`。Stage 1 不需要人体模型或 GPU；MHR 拟合可在 CPU 运行；Stage 2 的完整 Warp 仿真需要 NVIDIA GPU、驱动、CUDA Toolkit 和已编译 Warp。仅显式使用 `--body-model smpl` 时才需要 `basicmodel_m_lbs_10_207_0_v1.1.0.pkl`。

## 二、Docker 镜像（可选）

构建上下文为 `TemplatePattern_final`，GarmentCode 与 NvidiaWarp-GarmentCode 在构建阶段按固定提交下载，Warp 的 CUTLASS 子模块使用递归 submodule 初始化：

```bash
docker build --network=host --progress=plain \
  -f TemplatePattern_final/Dockerfile \
  -t templatepattern:latest \
  TemplatePattern_final
```

Dockerfile 使用 `uv` 创建 Python 3.12 虚拟环境，不再安装或调用 Conda。构建时会下载 MHR v1.0.1 TorchScript 模型，默认 Stage 2 不再挂载 SMPL 目录。
Docker 构建阶段默认以 root 执行，Dockerfile 内不需要 `sudo`。若宿主机用户无权访问
`/var/run/docker.sock`，可将用户加入 `docker` 组并重新登录；仅宿主机命令必要时使用 `sudo`。
若终端提示 `sudo: The "no new privileges" flag is set`，则该终端处于受限容器/沙箱，无法提权或访问宿主机
Docker daemon。应改用宿主机原生终端构建，或使用已配置好的远程 Docker context；这不是 Dockerfile 错误。

## 三、第一步：Stage 1

本地运行：

```bash
.venv/bin/python -m TemplatePattern_final stage1 \
  --style long_sleeve \
  --body-config config/body_long.json \
  --output-dir TemplatePattern_final/outputs/stage1/body_long
```

Docker 运行：

```bash
docker run --rm \
  -v "$PWD/body-configs:/data/inputs:ro" \
  -v "$PWD/TemplatePattern_final/outputs/stage1:/data/stage1" \
  templatepattern:latest stage1 \
  --style long_sleeve \
  --body-config /data/inputs/body_long.json \
  --output-dir /data/stage1/body_long
```

短袖将 `--style` 改为 `short_sleeve`。

当六项参数在 reference 覆盖能力内时，使用原始输入。当单项越界或参数组合没有任一模板能同时覆盖时，按木桶规则选择最大可满足/最接近 anchor。`requested_body_input` 保留原始值，`effective_body_input` 和 `body_input` 保存实际采用值。纸样、Stage 2 manifest 和 MHR 均使用实际采用值。当前身高直接覆盖 150–190cm，190–200cm 中超界的输入回退到选定的 190cm anchor，仍会产生明确结果。

产物固定目录：

```text
TemplatePattern_final/outputs/stage1/body_001/
├── stage1_manifest.json
├── pattern.json
├── pattern.svg
├── body_target.json
└── stage2_request.json
```

## 四、第二步：Stage 2

本地运行：

```bash
.venv/bin/python -m TemplatePattern_final stage2 \
  --stage1-dir TemplatePattern_final/outputs/stage1/body_long \
  --output-dir TemplatePattern_final/outputs/stage2/body_long/a30 \
  --pose a30
```

Docker GPU 运行：

```bash
docker run --rm --gpus all \
  -v "$PWD/TemplatePattern_final/outputs/stage1:/data/stage1:ro" \
  -v "$PWD/TemplatePattern_final/outputs/stage2:/data/stage2" \
  templatepattern:latest stage2 \
  --stage1-dir /data/stage1/body_001 \
  --output-dir /data/stage2/body_001/a30 \
  --pose a30
```

无 GPU 时可先执行 `--convert-only`；姿态可选 `a30`、`a45`、`a60`、`tpose`。

产物固定目录：

```text
TemplatePattern_final/outputs/stage2/body_001/a30/
├── stage2_manifest.json
├── input/
├── body/
└── simulation/
```

其中 `body/` 保存 MHR 生成并转换为 SMPL 6890 拓扑的 OBJ、YAML、`mhr_params.json`、兼容参数、局部校正和测量报告；`simulation/` 保存 BoxMesh、仿真 OBJ、正反面渲染和材质/纹理文件。

## 五、批量运行

```bash
.venv/bin/python -m TemplatePattern_final batch-stage1 \
  --style long_sleeve \
  --input-dir body-configs \
  --output-root TemplatePattern_final/outputs/stage1
```

```bash
.venv/bin/python -m TemplatePattern_final batch-stage2 \
  --stage1-root TemplatePattern_final/outputs/stage1 \
  --output-root TemplatePattern_final/outputs/stage2 \
  --pose a30 --resume
```

Stage 1 批量结果包含 `TemplatePattern_final/outputs/stage1/batch_manifest.json`；Stage 2 只读取该 manifest 和 Stage 1 任务包。

## 六、腕部摆放与参考拖拽

默认配置已关闭 `enable_cloth_reference_drag`，普通 Stage 2 命令无需增加参数。
需要重新开启做对照时，追加：

```bash
--sim-config TemplatePattern_final/assets/garmentcode_sim_props/reference_drag_on.yaml
```

长袖袖口使用当前人体手部／前臂分区交界的中心和局部方向定位；袖片绕既有袖口连接边刚体旋转，兼顾人体间隙与肩部连接位置。摆放步骤只改变三维平移和旋转，不修改二维纸样、缝合列表或材质；其前面的转换步骤负责传递 Stage 1 尺寸。
初始净间隙为仿真配置中的人体碰撞厚度＋布厚＋1 mm。`input/body_pose_relation.json` 的 `wrist_placement` 记录左右腕部、袖口周长、连接误差和袖片旋转；几何不足或无法安全摆放时明确失败，不自动缩放。

生成含人体的缝合场景、左右腕部近景和穿模统计（从项目父目录运行；输出目录必须是新的）：

```bash
Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final.sim.wrist_diagnostics \
  --stage2-dir TemplatePattern_final/outputs/stage2/body_long/a30 \
  --output-dir TemplatePattern_final/outputs/validation/wrist_placement/manual_check
```

输出包含 `wrist_report.json`、`initial_sewing/sewing_scene.obj`、`initial_body_cloth_seams.obj`、`final_body_cloth_seams.obj` 和 `initial/final_left/right.png`。OBJ 场景统一为米，左右按人体解剖侧命名；MTL 与 OBJ 一起保留。红色网格线表示缝合位置，近景红点表示穿入人体超过 0.1 mm 的袖口或缝合边顶点。报告另外检查三角面相交，不以顶点检测代替全部接触检查。

需要已有 BoxMesh 和顶点分区；仿真失败但已保存 `sim.obj` 的任务也能检查。穿模统计、视觉改善和引擎的静止／碰撞验收分别保留，不能互相替代。验证结果位于已忽略的 `outputs/`，不上传仓库。

## 七、Stage 1 到 Stage 2 的动态尺寸传递

Stage 2 不再直接使用固定尺寸的 reference。转换器读取 Stage 1 各母裁片的二维宽高，相对于 final 内同款 M 码模板计算横纵比例，再按固定语义对应关系更新 reference 子裁片的局部顶点：身片／过肩同名对应；袖片、袖口、领座、翻领对应各自母裁片。转换后才根据动态人体调整摆放。

保留 reference 的裁片拆分、顶点数量、边索引、缝合配对、缝合方向及局部曲线形状；同一母裁片的子裁片共享尺寸比例。当前是“尺寸变化”适配，不是任意新轮廓或新拓扑的直接导入。缺少必需母裁片或存在退化尺寸时明确失败，不静默退回固定 reference。

`input/conversion_debug.json` 的 `geometry_transfer.panels` 逐片记录 Stage 1 母裁片、基准宽高、横纵比例和最终宽高（分别明确 mm/cm）。原 Stage 2 命令不变，请使用新输出目录，旧结果不会自动更新。默认手部姿态、材质与碰撞参数保持不变；0.5 cm 同步偏移仍是独立预览，不纳入默认流程。

## 八、固定袖口裁剪与默认 Draco 产物

长袖在动态尺寸转换及人体摆放完成后，对每片袖口固定缩短 2 cm：保留袖片连接边，沿袖口长度轴压缩至自由端，不改变宽度、三维平移/旋转或缝合边索引。记录见 `input/body_pose_relation.json` 的 `cuff_trim`。短袖不处理；袖口长度不大于 2 cm 时明确失败，不生成退化裁片。每次 Stage 2 都从 Stage 1/reference 重新构造，不在已有裁剪结果上累计裁剪。

完整 Stage 2 默认导出 `simulation/scene_draco.glb`，包含人体和衣服，统一为米；沿用已验证的 `KHR_draco_mesh_compression` 格式，前端 Draco 解码器路径为 `/draco/`。GLB 为中性材质几何导出，不包含原彩色纹理。需要 `DracoPy==1.7.0`（已在 requirements-runtime.txt 声明）。可用 `--no-draco` 关闭；`--convert-only`／`--boxmesh-only` 默认不导出。

正常完成及质量检查失败但网格已保存的仿真都会尝试导出。质量失败仍返回失败状态及非零退出码，manifest 标记 `draco_quality: simulation_failed_quality_checks`，不得把 GLB 存在等同验收通过。`--archive` 生成的 `stage2_results.zip` 包含 GLB、报告及任务产物。运行异常时不尝试导出残留的旧网格。
