# 动态纸样、人体与仿真修复

本轮状态：代码修复及基础回归已落地，但完整成衣质量尚未通过，不能视为整个仿真重构已验收完成。最新长短袖均跑完默认 2400 步并成功渲染、归档，仍因未静止/穿体/自交标记失败。

最终 GPU 报告：`outputs/validation/gpu_final_20260911/gpu_validation_report.json`。该目录的 `short/`、`long/` 各含完整 body/input/simulation、failed manifest 和 stage2_results.zip；日志是 short.log、long.log。下一步需要对初始贴体包裹、领口及袖窿装配、约束与碰撞过程单独验收，不应继续靠增加步数或放宽质量阈值解决。

## 已修改

- Stage 1：同片轮廓、控制点、语义曲线使用同一缩放中心。原实现每条曲线独立居中缩放，单个控制点原地不动，是跨身高后缝线偏离轮廓的根因。旧 Stage 1 任务必须重新生成，不能仅重跑 Stage 2。
- Stage 2：不再以 reference specification 覆盖输入纸样；按连续语义边界配对，以约 5mm 等弧长采样缝线，避免微小细分边造成退化三角形。领片上下边先统一方向再分割；修复左右袖缝线映射，并补上腋下缝线。肩缝、袖山、侧缝根据源曲线实际端点高低配对，后背连接按横向端点配对，不假定长短袖、所有尺码的曲线存储方向相同。
- SMPL：去掉绝对 scale 0.75～1.35 限制；保留六项误差验收、有限正数输入检查。身高用 mm，不是 cm。
- 仿真：持久化最终 sim_props.yaml 和 simulation_report.json。未静止、穿体、自交超限不再标记成功；异常写 failed manifest，`--archive` 仍打包已有诊断产物。
- 渲染：PyOpenGL 3.1.0 与 pyrender 声明一致；注册 ctypes 参数处理器。全流程运行前执行带纹理 EGL 预检查。
- 摆放：绘图 Y 向下转为三维 Y 向上；长短袖躯干纸样共同依肩高、躯干范围及当前纸样高度摆放，不再使用固定的躯干高度。袖口按整条连接缝线中心对齐。仍需完整仿真与人工外观验收；不是已验证的贴体包裹算法。

## 材料参考

已读取用户指定的 shirt_asia_M_package/simulation，以及其 20260909_104101/stages/B/dynamics/sim_props.yaml。材料值与 final 一致，保留：

| 参数 | 值 |
| --- | --- |
| garment_tri_ka / garment_tri_ke | 10000 / 10000 |
| garment_edge_ke / garment_edge_kd | 1 / 10 |
| garment_tri_kd | 1 |
| spring_ke / spring_kd | 50000 / 10 |
| fabric_density / fabric_thickness / fabric_friction | 1 / 0.1 / 0.5 |

这些是当前 GarmentCode 厘米制仿真配置值，不是可直接解释为 SI 单位的实验材料参数。对齐参考设置：enable_attachment_constraint=false、纹理 dpi=150；不降低穿体、自交验收阈值。参考项目另有 wrapped 摆放及额外约束，不能只凭材料相同推断结果相同。

## 本轮测试

报告目录：`outputs/validation/heights_20260911/`。

- height_validation_report.json：150～220cm 每 10cm，长短袖、height_only/proportional，共 32 组 Stage 1 和转换均通过。
- boxmesh_height_report.json：150、180、220cm 等比例体型长短袖共 6 组通过网格生成。
- smpl_height_report.json：150、151、170、190、220、260cm 两种比例，共 12 组，11 组通过；260cm 且其余尺寸保持原样未达误差阈值，明确失败。
- 协议/无效输入报告/失败压缩包/统一缩放/动态纸样/袖山方向/Y 轴方向回归 8 项通过；另一次含 XS/L/XXXL 真实 SMPL 拟合及四姿态有限顶点检查的回归通过；带纹理 EGL 预检查通过。
- RTX 5090 长短袖各 20 步完成引擎运行、OBJ 保存、正反面渲染和 ZIP 归档；均未收敛且存在穿插，因此正确标记 failed。长袖首轮在 Y 坐标修正前执行，仅用于异常处理链路验证；短袖使用修正后的坐标。结果目录为 `outputs/validation/gpu_smoke_20260911/`，不是可交付的合格成衣。
- Draco 编码及从 GLB 提取压缩流后的解码测试通过；尚未在用户前端浏览器验收。
- 可查看 `outputs/validation/draco_export/scene_draco.glb`；它来自最终短袖的失败仿真，仅用于导出/解码测试，不代表成衣质量合格。

有限抽样不等于“所有身高 ≥150cm、任意其它尺寸都能成功”。有效输入应得到产物或明确的失败报告，不能对不可达体型或失败仿真伪造成功结果。

可复跑高度检查（指定新的空报告目录）：

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final.tests.validate_heights \
  --output-dir TemplatePattern_final/outputs/validation/my_height_check \
  --smpl-model-dir "$SMPL_MODEL_DIR" --boxmesh
```

## 运行命令

以下在 SewingPattern 目录运行。使用新输出目录，避免混入旧结果。

```bash
Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final stage1 \
  --style long_sleeve --body-config TemplatePattern_final/config/body_long.json \
  --output-dir TemplatePattern_final/outputs/stage1/body_long_fixed

Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final.sim.render_support

Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final stage2 \
  --stage1-dir TemplatePattern_final/outputs/stage1/body_long_fixed \
  --output-dir TemplatePattern_final/outputs/stage2/body_long_fixed/a30 \
  --pose a30 --smpl-model-dir "$SMPL_MODEL_DIR" --archive
```

`SMPL_MODEL_DIR` 应设置为含 basicmodel_m_lbs_10_207_0_v1.1.0.pkl 的目录。Stage 2 的 body/、input/、simulation/、stage2_manifest.json、stage2_results.zip 全部位于指定 output-dir 下。20 步冒烟可加 `--max-sim-steps 20`，但未达到静止条件会返回失败，这是预期的质量判断，不能作为完整仿真成功。

## Draco

安装：`uv pip install --python Bench2SewingPattern/.venv/bin/python DracoPy==1.7.0`。完整 Stage 2 加 `--draco --archive`，产生 `simulation/scene_draco.glb` 并纳入 ZIP。

也可独立导出现有 OBJ，不重跑仿真：

```bash
Bench2SewingPattern/.venv/bin/python -m TemplatePattern_final.sim.draco_export \
  --garment TemplatePattern_final/outputs/stage2/body_long_fixed/a30/simulation/sim.obj \
  --body TemplatePattern_final/outputs/stage2/body_long_fixed/a30/body/body.obj \
  --output TemplatePattern_final/outputs/stage2/body_long_fixed/a30/simulation/scene_draco.glb
```

目前 GLB 是中性材质的几何导出，不嵌入原纹理；ZIP 保留原材质纹理。衣服厘米转米、人体保持米。独立导出不改变原任务的成功/失败状态，也不会自动重建既有 ZIP。

前端应配置 `dracoLoader.setDecoderPath('/draco/')` 并将它传给 GLTFLoader；该 URL 目录需由前端实际部署 decoder JS/WASM。ZIP 不能交给 Draco 解码，GLB 才使用 [KHR_draco_mesh_compression](https://github.com/KhronosGroup/glTF/blob/main/extensions/2.0/Khronos/KHR_draco_mesh_compression/README.md)。GLB 内 extras 仅记录路径提示，不会自动配置前端加载器。
