# 运行问题与动态 SMPL 审查

本次为代码审查和实测，不修改生产运行逻辑。对照范围：final 两阶段 CLI、人体拟合和 LBS、GarmentCode 调用、v2 的 mesh/smpl_body.py 与 scripts_v2 拟合/构建脚本，以及 v1 的纸样模板实现。未重新执行完整 GPU 仿真。

## 结论

final 的人体确实动态生成：Stage 1 body_target.json 的六项毫米尺寸转换为厘米目标，拟合 scale 与十个 betas，再对 canonical 网格施加校正，重新计算 joints 并执行 LBS。不是直接复用固定 OBJ。但完整流水线不能验收为正确：Stage 2 会覆盖动态纸样，仿真失败没有正确传递到任务状态，渲染依赖冲突仍存在。

## 本次日志

- 1500 帧后 9629/12522 顶点未静止，633 次人体相交、3626 次自交，质量不合格。
- 仿真保存 sim.obj 后在 glGenTextures 失败。末尾是 ctypes 参数 handler 查找失败，而不是足以证明 EGL 创建失败的日志。
- 使用 Bench2SewingPattern/.venv 的 PyOpenGL 3.1.0，无需 GPU、无需创建上下文，直接对 ctypes.byref(ctypes.c_uint(0)) 调用 GLuintArray.getHandler 即复现相同异常；临时注册 CtypesParameterHandler 后最小测试通过。完整渲染仍待验证。
- 先前把异常直接归因为 EGL 不可用不够准确；仅设 EGL 无法保证修复 Python 层 handler 问题。
- 日志的命令换行若是真实输入而非排版，models 后需要反斜杠续行，否则 --archive 不属于同一命令。1500 帧日志与展示命令可能并非完全对应。

## 按优先级排列的问题

### P0：Stage 2 覆盖了 Stage 1 的动态纸样

sim/spec_converter.py::_apply_reference_spec 在 panel 名称集合相同时，直接以 reference 的 panels 和 stitches 替换整个生成结果，而不仅借用缝合规则。实测：将 reference 同名纸样的一个顶点移动 10 个单位，两份输入经过此函数后完全相同。

结果是人体会变，但进入仿真的衣服可能还是固定尺寸。应保留 Stage 1 几何，以明确的边对应关系转换 stitches，不能简单删掉覆盖语句就宣称修复了缝合。

### P0：仿真质量失败仍可能标记成功

GarmentCode run_sim 将不收敛和过多碰撞写入 props.stats.fails，继续保存并渲染。final 不读取这些失败标志，stage2_pipeline.py 的 manifest/status 固定写 completed。产物缺失也可记为 null，不构成强制验收失败。

sim_props.yaml 在仿真前保存，final 没有在 run_sim 返回后重新持久化最终统计。渲染抛异常时，不会写 Stage 2 manifest，也不会生成 archive。应分别记录 simulation_quality、render_status、任务失败原因和已有产物。

### P1：安装依赖自相矛盾

requirements-runtime.txt 同时固定 pyrender==0.1.45 与 PyOpenGL==3.1.7；已安装 pyrender 的 METADATA 明确声明 PyOpenGL==3.1.0。常规 pip/uv 解析会遇到冲突。不能把手动升级 OpenGL 当作整套 requirements 已验证。需选择经过验证且元数据一致的依赖或受控补丁策略，并先做小型渲染测试再执行长仿真。

### P1：摆放与人体动态变化不完全配套

tpose_placement.py 的 LONG_TORSO_PRESET 为躯干和领子设置固定坐标；袖子、袖口才使用人体 landmarks。人体变化后，领口与躯干位置不能保证对应。日志把多个 collar 面片归到 left_arm；该归属参与 collision filters/reference drag，值得优先检查。它是可疑线索，尚未通过对照仿真证明就是穿模的唯一原因。

### P1：固定约束标签缺失

生成规格没有 edge labels，配置却要求 lower_interface/right_collar/left_collar/strapless_top；运行时 attachment 被关闭。修改 attachment_frames 不会补出标签。应建立符合衬衫实际结构的固定点规则，并校验输出 vertex_labels。

### P1：局部校正与验收协议仍有缺口

- apply_body_correction 的臂长校正直接缩放 Arm/ForeArm/Hand 分割中的顶点，未平滑融合到肩部，且是三轴缩放，不是严格沿肩腕轴的校正。
- 围度/肩部校正按高度作用于整张网格，未严格限制躯干或肩带，可能影响同高度的手臂。
- 拟合时各因子 clip 到 0.85..1.15，不等于组合后的每个局部形变都限制在 15%；也没有单独拒绝“未经 clip 的请求超过 15%”。
- body_params 兼容路径无条件 accepted=True，不会针对目标误差再次验收。
- 围度使用固定身高比例平面 0.72/0.55/0.48，肩宽、臂长使用分割统计代理值；不能将内部误差很小等同于真实人体测量精度已验证。
- 围度函数加总所有交线段，未识别独立闭环，并保留凸包 fallback。极端体型、切面退化仍需测试。

### P2：批量恢复及容器检查

- resume 摘要仅检查 Stage 1 digest/文件存在，未覆盖 sim-config、模型版本、max-sim-steps、convert-only 等执行设置；转换任务可能被当成完整仿真完成而跳过。
- batch-stage2 的 Stage1Bundle.load 在逐任务 try 外，一个坏任务包可能中断全部任务。
- _write_batch_manifest 对只有 blocked 的批次仍可标 completed。
- Docker 检查依赖 TEMPLATEPATTERN_IN_DOCKER=1，但当前 Dockerfile 未设置它。
- read_json/write_json 相对路径以 final 根目录解释；单任务输出已 resolve，但批量 manifest 等仍有相对路径处理不一致风险。

## v1 / v2 / final 对照

| 版本 | 实际能力 | 限制 |
|---|---|---|
| v1 | 以尺码、语义关键点、曲线/放码规则生成二维纸样 | 本次 Python 源码扫描未找到独立 SMPL 动态生成链，不能作为 SMPL 验收基准 |
| v2 | mesh/smpl_body.py 通过 smplx/torch 使用 betas、pose；scripts_v2 有自实现 LBS 与尺寸拟合 | 独立拟合脚本默认 4 betas，scale 0.90..1.08；带宽选点加凸包测量，主要报告求解器状态 |
| final | 自包含 NumPy SMPL loader/LBS；十个 betas、按目标身高求 scale、切面测量、残差校正和六项容差 | 人体与纸样端到端未正确衔接；校正平滑性和独立测量验收不足 |

## 动态验证

在用户指定的 Bench2SewingPattern/.venv 中，通过 unittest 执行 tests/test_pipeline.py，显式配置真实 SMPL 模型：4 个测试全部通过，耗时 62.773 秒。动态测试实际执行 XS、L、XXXL 的拟合、阈值、不同 betas、不同 canonical vertices 检查，而不是跳过 SMPL。

另对当前样例只改变一个 beta，执行 posed_vertices：beta 0/1/2 分别加 1，最大顶点位移约 8.54/3.96/1.55 cm，均为 6890 个有限顶点。模型具有 13776 个固定三角面，LBS 没有重建拓扑。

再次逐尺寸拟合并执行 LBS 的结果（max_nfev=60；误差来自当前内部测量器）：

| 尺寸 | scale | 六项最大绝对误差/cm | 相比上一尺寸最大顶点位移/cm | 有限顶点/面 |
|---|---:|---:|---:|---|
| XS | 1.145682 | 0.010265 | — | 6890 / 13776 |
| L | 1.131563 | 0.008483 | 8.9696 | 6890 / 13776 |
| XXXL | 1.170711 | 0.006348 | 8.9960 | 6890 / 13776 |

三组 betas 不同，校正缩放因子却全部为 1：纯 SMPL 已满足当前阈值，因此不需要校正。这是合理结果；不能要求所有不同体型的 correction 因子必须不同。现有测试比较整份 correction 字典，其中包含不同目标值，所以其通过并不能证明非恒等校正路径已经被覆盖。这三组也没有验证肩臂校正的平滑性。

同一份 fitted params/correction 下切换 a30/a45/a60/tpose，输出均有限；高度分别约 1.76616/1.76678/1.76753/1.76500 m。此测试复用了拟合结果，不表示所有四姿态完整 CLI/GPU 流程已经验收。

当前样例存档报告六项误差最大 0.00827 cm，但 optimizer.success=false（达到评估次数上限）。在六项误差通过的前提下，这不应单凭 solver.success 判定人体失败。反过来，solver.success 也不代表测量合格。

## 3D 压缩要求（用户补充）

前端解码器路径要求为 /draco/。当前 --archive 仅生成 ZIP，不能直接用 Draco 解码器读取；代码只声明 sim.glb 路径，没有实际 Draco 编码实现。

应新增 Draco 压缩 glTF/GLB 作为查看器产物，并保留 ZIP 作为整任务打包。验证 glTF 包含 KHR_draco_mesh_compression、前端 /draco/ 资源可访问、解码后的几何/UV/材质及人体与服装单位一致。body.obj 使用米而服装 OBJ 使用厘米，合并 GLB 必须统一单位。/draco/ 是查看器解码器资源路径，并不是 ZIP 解压目录。

## 建议修复顺序

1. 停止 reference 覆盖动态纸样，建立几何与缝合对应验收。
2. 修正 PyOpenGL handler/依赖冲突，加入独立渲染检查和已有 sim.obj 的单独渲染入口。
3. 正确传递仿真质量状态、失败 manifest、最终统计和已有产物。
4. 修正躯干/领口摆放与标签；短仿真对照后再跑完整仿真。
5. 完善校正约束和独立测量、批量恢复，再实现 Draco GLB 导出与前端解码验收。
